"""Officially supported but optional helpers around the wallow core.

Modules here are first-class (tested, semver-stable) but live outside the top
level so that the core API stays state-machine-free for users who want to roll
their own dispatch flow.
"""
